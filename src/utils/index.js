"use strict";

const {getRandomInt} = require(`./get-randim-int`);
const {shuffle} = require(`./shuffle`);
const {getFormatedDate} = require(`./get-formated-date`);

module.exports = {
  getRandomInt,
  shuffle,
  getFormatedDate,
};
